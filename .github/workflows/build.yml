name: Build

on:
  push:
    branches:
      - main
    tags:
      - 'v*' # Trigger automatico per i tag di versione (es. v1.0.0)
  pull_request:
  # Abilita il trigger manuale tramite il pulsante "Run workflow" su GitHub
  workflow_dispatch:

jobs:
  build:
    name: Build ${{ matrix.target.goos }}/${{ matrix.target.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
          - goos: windows
            goarch: amd64
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: windows
            goarch: arm64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          # Ottimizzazione: mantiene la cache delle dipendenze per build più veloci
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Build binary
        env:
          GOOS: ${{ matrix.target.goos }}
          GOARCH: ${{ matrix.target.goarch }}
        # Generazione dei binari con nomi specifici per piattaforma
        run: |
          BINARY_NAME=agent-go-${{ matrix.target.goos }}-${{ matrix.target.goarch }}
          if [ "${{ matrix.target.goos }}" = "windows" ]; then
            BINARY_NAME="${BINARY_NAME}.exe"
          fi
          # Build del comando principale
          go build -v -o "bin/${BINARY_NAME}" ./cmd/main.go
          # Salvataggio dei nomi nelle variabili di ambiente del workflow
          echo "BINARY_PATH=bin/${BINARY_NAME}" >> $GITHUB_ENV
          echo "BINARY_NAME=${BINARY_NAME}" >> $GITHUB_ENV

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.BINARY_NAME }}
          path: ${{ env.BINARY_PATH }}

  # Job dedicato alla creazione della Release (eseguito solo sui tag)
  release:
    name: Create GitHub Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: ./artifacts/**/*
          generate_release_notes: true
          # Nome della release basato sul tag
          name: Release ${{ github.ref_name }}


